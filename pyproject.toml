[project]
name = "endless-ball-machine"
version = "0.1.0"
description = "Endless Ball Machine prototype"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "pymunk>=7.0.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
